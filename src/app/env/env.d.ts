declare namespace Environment {
  const appVersion: string;
  const debugMode: string;
  const clientId: string;
  const webApiUrl: string;
  const tokenEndPointUri: string;
  const refreshTokenLiftime: string;
  const enableAppInsights: string;
  const appInsightsInstrumentationKey: string;
  const livechaturl: string;
  const domain:string;
  const environment:string;
  const analyticsToken: string;
  const allowAnalytics: string;
}